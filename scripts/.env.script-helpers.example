# Example environment file for MCP script helpers
# Copy this file to 'scripts/.env.script-helpers' (git-ignored) and fill in real values.
# NEVER commit actual secrets or tokens.

# MCP server base URL
MCP_URL=http://localhost:9000/mcp

# Authorization headers (placeholders only)
MCP_CONFLUENCE_AUTH_HEADER=Token <REPLACE_ME>
MCP_JIRA_AUTH_HEADER=Token <REPLACE_ME>

# Optional link code header used by some MCP servers
MCP_LINK_CODE=<REPLACE_ME>

# TLS verification (true | false)
MCP_VERIFY_TLS=true

# Request timeout in seconds
MCP_TIMEOUT_SECONDS=30
